# 我的题库模块 — Vibe Coding 设计摘要

> 用途：直接复制本文档喂给 AI 编码工具（Cursor / Claude Code / v0），生成可运行组件。

## 技术栈约束

- React（函数组件 + useState/useEffect）
- Tailwind CSS
- 图标用 lucide-react
- 单文件组件即可起步，三个子题库内容分别拆成子组件（GeneralBank / ExclusiveBank / CustomBank），共用一个 QuestionCard 组件

## 结构锚点（页面骨架）

```
我的题库（顶层容器）
└─ Tab 横切：通用题库 | 专属题库 | 定制题库
   └─ 每个 Tab 内固定结构：状态条 → 操作区 → 题目列表
```

三个 Tab 平级，不做二级菜单。默认进入 Tab = 用户上次访问的，新用户首次默认落地"通用题库"。

## 视觉锚点

- Tab 选中态：底部边框高亮（border-bottom: 2px solid 主色），未选中文字用次要灰色
- 题目卡片：白底 + 1px 边框 + 圆角，内部一行布局 = 分类 badge + 题干文字 + 右侧 chevron 展开图标
- 分类 badge：浅色背景 + 深色文字，不同分类用不同色（如：行为面=紫色系，场景题=青色系，项目深挖=橙色系，高频考点=粉色系）
- 上传区：虚线边框卡片，未上传时显示上传图标+引导文案，已上传显示文件图标+文件名+状态文字
- 生成按钮：主色填充，宽度 100%，生成中变为禁用态+spinner+阶段文案
- 岗位选择器：可搜索下拉 + 下方横向 tag 列表展示已选岗位，每个 tag 可关闭

## 三个子模块的状态机

### 1. 通用题库（自动拉取，无生成动作）

状态：`无岗位` → `已选岗位（自动展示列表）`

- 顶部岗位选择器（可搜索下拉），选定后立即触发后台拉取，无需用户点"生成"
- 支持多岗位：已选岗位以横向 tag 列表展示，点击切换，可继续添加
- 题目列表按分类分组展示（行为面/技术面/场景题/算法等）
- 文案要强调"自动"，例如选择器下方提示「已为该岗位自动匹配 N 道真题，每日更新」

### 2. 专属题库（上传简历生成）

状态：`空状态` → `已上传待生成` → `生成中（分阶段）` → `已生成` → `重新生成`

- 双上传位：简历（必填）+ 项目材料（选填，可多文件）
- 上传后每个文件独立显示解析状态（解析中/完成/失败可重传）
- 点击"生成"按钮后进入分阶段进度提示：分析简历内容 → 提取项目经历 → 生成匹配题目（每阶段 2-3 秒切换文案，避免空转 loading）
- 生成完成展示题目列表，每题可展开看 AI 生成的参考回答
- 支持单题重新生成 + 整体重新生成
- 支持多份简历对应不同岗位（多题库实例，类似通用题库的多 tag 切换）

### 3. 定制题库（上传JD生成）

状态：`空状态` → `生成中` → `已生成`

- 输入方式：textarea 粘贴 JD 文本（默认主入口）+ 次要的文件上传入口
- 生成完成后自动用「公司名 + 职位名」命名该题库实例
- 题目列表额外带"高频考点"等差异化标签，避免用户觉得跟专属题库一样
- 同样支持单题/整体重新生成

## 共享组件

**QuestionCard**
```
props: category (string), categoryColor (string), questionText (string), answerText (string), defaultExpanded (bool)
行为: 点击整行展开/收起 answerText，chevron 图标旋转180度
```

三个子模块的题目列表都复用这一个组件，只是传入的数据和 badge 颜色不同。

## 建议的数据结构（喂给生成代码时一并给出）

```js
// 通用题库
{ jobId, jobName, questions: [{ id, category, questionText, answerText }] }

// 专属题库实例（支持多份）
{ id, name, resumeFile, materialFiles: [], status: 'empty'|'uploading'|'generating'|'ready', questions: [] }

// 定制题库实例（支持多份）
{ id, companyName, jobTitle, jdText, status: 'empty'|'generating'|'ready', questions: [] }
```

## 待确认的产品决策（交给后端/产品定，不影响前端先行开发）

1. 通用题库是否真的需要支持多岗位同时管理，还是单一求职目标即可
2. 专属题库/定制题库的多实例是否要做版本化存储，还是简单覆盖
3. 生成任务是否走异步+消息通知，还是同步等待（决定前端 loading 策略的复杂度）
